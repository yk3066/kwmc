import 'package:flutter/material.dart';

class kodagu extends StatefulWidget {
  const kodagu({Key? key}) : super(key: key);

  @override
  State<kodagu> createState() => _kodaguState();
}

class _kodaguState extends State<kodagu> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
